-- Migration 001: Initial schema
-- This file is intentionally empty because the initial schema
-- is applied by database/schema.sql via the embedded migration runner.
-- Future migrations go here as separate files: 002_*.sql, 003_*.sql, etc.

-- Example of what a migration looks like:
-- ALTER TABLE bookmark ADD COLUMN color TEXT;
-- INSERT INTO schema_version (version) VALUES (2);
